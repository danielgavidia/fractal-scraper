services:
    - type: cron
      name: fractal-favorites-cron
      env: node
      plan: free
      schedule: "0 12 * * *"
      command: "npm run worker"
      region: oregon
      buildCommand: "bun install"
